bundle:
  name: tabpfn_databricks

variables:
  project_name:
    description: "Name of the project"
    default: "tabpfn-databricks"

targets:
  dev:
    default: true
    mode: development
    workspace:
      host: https://e2-demo-field-eng.cloud.databricks.com/

# Sync notebooks and other files to the workspace
sync:
  include:
    - "notebooks/*.ipynb"
